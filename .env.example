# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL="database://databaseUser:databasePassword@localhost:port/databaseName?schema=public"

# Secret JWT  https://www.md5hashgenerator.com/
JWT_SECRET="Your secret JWT"

# db connection credentials, used in the docker-compose file
POSTGRES_USER=databaseUser
POSTGRES_PASSWORD=password
POSTGRES_DB=DatabaseName

# this is the conection to the printer
PRINTER_INTERFACE=printer_interface

# API PORT
PORT="API_PORT"

# cloudinary
CLOUDINARY_NAME=name
CLOUDINARY_API_KEY=api_key
CLOUDINARY_API_SECRET=api_secret